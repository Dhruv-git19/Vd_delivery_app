import 'package:flutter/material.dart';
import 'package:vedasip_delivery_app/feature/verification_screen/verification_screen.dart';

ThemeData lightTheme = ThemeData(
  useMaterial3: true,
  appBarTheme: AppBarTheme(elevation: 0),
);

const textfieldColor = Color(0x2B97E3CE);
const primaryColor = Color(0xFF4BC7A4);
const secondaryColor = Color(0xFF256150);
const verificationColor = Color(0xFFF5FFFC);
const verifyheadingcolor = Color(0xFF6B9080);
const verifybuttonBorderColor = Color(0xFFE0E0E0);
